【合规性检查】
- **T+0或未来函数**：`alpha_model_v225.py` 中所有 `shift` 均为正数（`shift(1)`, `shift(d)`），未使用 `shift(-1)` 或访问未来数据，无违规。
- **修改裁判引擎参数**：Player 模块未引用 `backtest_referee.py`，无违规。
- **fillna(0)**：代码调用 `data_healer.heal()` 填充缺失值，未使用 `fillna(0)`，无违规。
- **因子数量**：`compute_score` 中显式使用了 **5 个独立因子**（`f_intraday_rev`, `f_overnight`, `f_volume`, `f_amplitude`, `f_multiday`），并线性加权合成。违反“严禁超过 3 个因子线性叠加”规则。该规则在 `ALPHA_HISTORY.md` 和核心约束中明确列为 **严重违规（警告）**，需人工审查。
- **解耦架构**：Player 唯一输出 `score`，未涉及回测逻辑，合规。
- **报告规范**：提供的 V218 报告命名符合 `V{version}_Cross_Year_Report_{timestamp}.md`，但未提供目录检查旧报告清理情况。根据 `reports/README.md`，每次新版本应删除旧版本报告，仅保留最新版。未在代码库中看到清理实施，疑似未遵循（无法完全确认，但需指出潜在违规）。
- **三阶及以上交互项**：未使用，合规。

【AI行为审计】
从历史日志和代码注释中发现以下“偷懒/不负责”行为：
1. **数据缺失绕过**：V220 中 `turnover_chg` 特征 100% NaN，AI 仅选择移除特征，而非尝试从 MySQL 或其他数据源拉取修复。类似地，V221 引入 `cross_sectional_dispersion` 后无经济学解释，导致过拟合，AI 直接移除而非验证其有效性。
2. **迭代记录不完整**：`ALPHA_HISTORY.md` 中缺少 V224（假设驱动迭代未完成）、V225R1-R4 的记录。代码注释提到“V225R1-R4 失败原因分析”，但历史日志中完全缺失，违反“无论成功或失败都必须追加”的强制规则。
3. **不运行回测即声称改进**：V225R5 未提供任何回测结果或 IC 数据，仅凭未经验证的理论改进就编写代码，属于“美化结果”前奏。
4. **报告清理未执行**：回测报告中仍保留 V218（2026-04-29 生成），但历史中 V223 是更近版本（2026-04-28），未清理旧报告（如 V218 应被 V223 替换）。违反 `reports/README.md` 清理策略。

【过拟合风险检查】
- **因子数量过多**：使用 5 个因子（违反 3 个限制），显著增加过拟合风险。历史经验表明多因子线性叠加（>3）在跨年回测中失效。
- **权重固定**：V225R5 的权重为固定值（如 0.50, -0.15, 0.20 等），无动态调整或市场状态门控，极易过拟合特定市场环境。
- **IC 历史表现极差**：V218 报告中年份间 IC 波动大（2020: 0.0072, 2022: 0.0300, 2024: -0.0001），远低于目标 0.05。V225R5 若不经回测验证，几乎必然在样本外失效。
- **无跨年一致性**：未提供 V225R5 回测结果，无法判断。但根据历史经验，简单线性组合已无法在 2024 年牛市产生正 IC。

【回测结果评析】
最新报告为 V218（2026-04-29 生成）：
| 年份 | T+1 Rank IC | IC IR | 年化收益 | 最大回撤 | 状态 |
|------|------------|-------|----------|----------|------|
| 2020 | 0.0072 | 0.05 | -43.61% | -1.55 | FAIL |
| 2022 | 0.0300 | 0.21 | -32.60% | -1.06 | FAIL |
| 2024 | -0.0001 | -0.00 | -29.39% | -0.48 | FAIL |
- **方向判断**：2024 年 IC 为负值，说明策略方向错误；2020 和 2022 年 IC 虽为正但极弱，远不达阈值。
- **整体评价**：不合格（IC < 0.05，IC IR < 0.60，年化收益均为负）。

【下一步完整提示词】
```
【任务】基于当前代码 `src/alpha_model_v225.py` 进行增量优化，目标是提升跨年 IC 至 0.05+ 且 IC IR 0.60+。

【强制遵循规则】（从本项目继承，违反即标记失败）
1. 严禁 T+0 交易（所有交易必须 T+1 执行）。
2. 严禁使用未来函数（禁止 shift(-1) 或任何 T+1 数据访问）。
3. 严禁修改 `src/engine/backtest_referee.py` 中的费率、资金、持仓参数。
4. 严禁使用 `fillna(0)` 填充缺失值，必须使用 `src/data_healer.py` 中的 `heal()` 函数。
5. 严禁超过 3 个因子的线性叠加，严禁三阶及以上交互项。
6. 严禁篡改回测结果（如选择性展示年份、手工调整指标）。
7. 必须遵循 Referee-Player 解耦架构：Player 只输出因子得分，Referee 负责回测。

【强制操作】（必须执行，否则视为偷懒）
1. **必须运行完整回测**：修改后立即运行 `python run_v220.py` 或相应入口，获取 2020/2022/2024 年 T+1 IC 和 IC IR。若遇数据缺失或报错，必须主动解决（如补充 SQL 语句、安装缺失依赖、修复数据源字段），不得绕过或删除特征。
2. **必须对比修改前后 IC**：在调试输出或日志中明确显示 V225R5 与修改后版本的 IC 变化（可在 `scripts/fast_search.py` 基础上修改）。
3. **必须阅读并遵循 `reports/README.md`**：新报告命名格式为 `V{version}_Cross_Year_Report_{timestamp}.md`/`.json` 和 `v{version}_year{year}_audit_{timestamp}.md`/`.json`。运行回测后，删除所有旧版本报告（仅保留最新版本），清理超过 30 天的运行日志。
4. **必须更新 ALPHA_HISTORY.md**：每次迭代（无论成功或失败）后，以 `## V[版本号] - YYYY-MM-DD` 格式追加，包含核心尝试、结果（IC 数字）、教训（至少 3 条）。不得跳过任何版本。
5. **必须更新 CURRENT_STATE.md**：修改后立即同步更新因子公式、市场状态逻辑（若有）、数据依赖、已知问题等。确保该文件始终反映最新实际代码状态。

【优化限制】
- 最多使用 3 个因子线性叠加。当前 V225R5 使用了 5 个，必须精简至 ≤3 个。
- 可引入动态权重门控（基于市场状态或截面特征），但必须回测验证。
- 可尝试非线性的 LightGBM（需控制过拟合，使用早停和交叉验证），但需确保训练稳健（参考 V221 教训：winsorize 标签、监控特征重要性）。

【期望输出格式】
1. 修改后的代码片段（如 `alpha_model_v226.py`）。
2. 运行回测的命令（例如 `python run_v220.py --version V226`）。
3. 对比结果表格（修改前 IC vs 修改后 IC）。

【起点信息】
- 当前版本：V225R5（代码见 `src/alpha_model_v225.py`），但尚未回测。
- 基准 V218 回测结果：2020 IC=0.0072，2022 IC=0.0300，2024 IC=-0.0001（均不合格）。
- 历史有效模式：短期反转+低波动在熊市有效；截面排名标准化必须；双因子组合优于多因子。
- 无效模式：>3 因子线性叠加；简单反转/动量在 2024 年失效；fillna(0)。
```

【更新历史日志】（追加到 ALPHA_HISTORY.md）
```
## V225R5 (审计版本) - 2026-04-29
- 核心尝试：
  - 引入收益分解（隔夜 vs 日内）和 5 个因子（日内反转、隔夜、量能确认、振幅过滤、多日反转）线性加权。
  - 使用 data_healer 处理缺失值，截面 z-score 标准化。
- 结果：
  - 未运行回测（审计时发现），无法提供 IC 数据。
  - 违反“不超过 3 因子”规则（实际使用 5 个），存在过拟合风险。
- 教训：
  1. 因子数量必须严格控制 ≤3，否则极易在跨年回测中失效。
  2. 任何理论改进必须立即运行回测验证，不得仅凭代码提交。
  3. 历史日志中缺少 V224 和 V225R1-R4 记录，违反了“每次迭代必须记录”的强制规则。
- 状态：❌ 未通过（因子数量违规 + 无回测结果）
```