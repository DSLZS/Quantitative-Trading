### 第一阶段：数据基座（进行中）
[x] 架构搭建：建立项目目录、配置环境、编写 .clinerules。

[x] 工具开发：完成 db_manager (数据库) 和 factor_engine (计算器)。

[ ] 数据灌入：运行同步脚本，将历史日线数据存入本地 MySQL。

### 第二阶段：特征工程（即将开始）
[ ] Pipeline 构建：编写 feature_pipeline.py，实现 MySQL -> 因子计算 -> Parquet文件 的自动化。

[ ] 数据清洗：在计算过程中处理停牌、空值和极值。

[ ] 特征宽表生成：生成一个包含数千行股票、数十个因子的“大数据集”存为 Parquet。

### 第三阶段：AI 模型训练
[ ] 样本切分：按时间顺序切分训练集（2020-2023）和验证集（2024）。

[ ] 模型调优：运行 model_trainer.py，训练 LightGBM 模型，观察特征重要性。

[ ] 模型持久化：将训练好的 .txt 或 .pkl 模型文件保存到 data/models/。

### 第四阶段：策略回测（核心空缺）
[ ] 编写 Backtester：创建一个回测引擎。

[ ] 逻辑实现：模拟每天根据模型预测分数买入前 10 名股票，卖出不在前 10 名的股票。

[ ] 绩效评估：计算夏普比率、最大回撤、年化收益率。

### 第五阶段：自动化推理（生产化）
[ ] 编写预测脚本：输入最新交易日数据，输出明天的“买入清单”。

[ ] 日志与监控：记录每天的预测结果和模型运行状态。